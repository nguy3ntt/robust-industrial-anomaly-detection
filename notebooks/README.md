# Notebooks

Use notebooks for disposable exploration and visual checks only. Reusable data
loading, metrics, models, and transformations belong under `src/` with tests.
Clear large outputs before committing a notebook.
